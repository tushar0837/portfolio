# FastAPI Chat Backend URL
# Default: http://localhost:8000
# Production: Your deployed FastAPI URL
REACT_APP_CHAT_API_URL=http://localhost:8000

# Localhost Development Token (required for localhost development)
# This token is required when API URL is localhost
# Generate: python3 -c "import secrets; import string; chars = string.ascii_letters + string.digits + '!@#$%&*'; print(''.join(secrets.choice(chars) for _ in range(30)))"
REACT_APP_LOCALHOST_TOKEN=your_localhost_token_here
